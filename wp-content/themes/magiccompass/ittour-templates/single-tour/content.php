<?php
/**
 * Display Single Tour content
 *
 * @package WordPress
 * @subpackage Magiccompass/ITtour
 * @since 1.0
 */

if ( ! defined( 'ABSPATH' ) ) {
    exit; // Exit if accessed directly.
}
?>
<div class="single-tour__content">
    <?php
    // ittour_show_template('single-tour/title.php', array('tour_info' => $tour_info));
    // ittour_show_template('single-tour/price.php', array('tour_info' => $tour_info));
    // ittour_show_template('single-tour/meta.php', array('tour_info' => $tour_info));
    // ittour_show_template('single-tour/flights.php', array('tour_info' => $tour_info));
    // ittour_show_template('single-tour/hotel.php', array('tour_info' => $tour_info));



//    unset($tour_info['key']);
//    unset($tour_info['id']);
//    unset($tour_info['tour_id']);
//    unset($tour_info['stop_sale']);
//    unset($tour_info['country_id']);
//    unset($tour_info['region_id']);
//    unset($tour_info['transport_type']);
//    unset($tour_info['hotel']);
//    unset($tour_info['hotel_rating']);
//    unset($tour_info['meal_type']);
//    unset($tour_info['country']);
//    unset($tour_info['region']);
//    unset($tour_info['currency_id']);
//    unset($tour_info['price']);
//    unset($tour_info['prices']);
//    unset($tour_info['flights']);
//    unset($tour_info['hotel_info']);

    var_dump($tour_info);
    ?>
</div>